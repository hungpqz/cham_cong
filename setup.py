from setuptools import setup, find_packages

setup(
    name='cham_cong',
    version='0.0.1',
    description='Ứng dụng chấm công nhận dữ liệu từ máy chấm công',
    author='hungpq',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False
)
